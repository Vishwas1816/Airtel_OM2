<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Address Form</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f2f2f2;
      display: flex;
      justify-content: center;
      padding: 40px;
    }

    .form-box {
      background: #fff;
      padding: 25px 30px;
      border-radius: 8px;
      width: 550px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
    }

    .form-box h2 {
      margin-bottom: 20px;
      font-size: 22px;
      text-align: center;
    }

    .form-box button.location-btn {
      background-color: #0066cc;
      color: #fff;
      border: none;
      padding: 10px;
      width: 100%;
      font-size: 15px;
      border-radius: 4px;
      margin-bottom: 20px;
      cursor: pointer;
    }

    .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
    }

    .form-grid input,
    .form-grid select {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 14px;
      width: 100%;
    }

    .form-row {
      margin-top: 15px;
    }

    .address-type {
      display: flex;
      gap: 20px;
      margin: 15px 0;
    }

    .btn-row {
      display: flex;
      justify-content: space-between;
      margin-top: 20px;
    }

    .btn-row button {
      padding: 10px 25px;
      border: none;
      border-radius: 4px;
      font-size: 15px;
      cursor: pointer;
    }

    .btn-save {
      background-color: #007bff;
      color: white;
    }

    .btn-cancel {
      background-color: #d9d9d9;
      color: #333;
    }

  </style>
</head>
<body>

<div class="form-box">
  <h2>Address Form</h2>
  <button class="location-btn">Use my current location</button>
  
  <div class="form-grid">
    <input type="text" placeholder="Name">
    <input type="text" placeholder="10-digit mobile number">
    <input type="text" placeholder="Pincode">
    <input type="text" placeholder="Locality">
    <input type="text" placeholder="Address (Area and Street)" style="grid-column: span 2;">
    <input type="text" placeholder="City/District/Town">
    <select>
      <option value="">Select State</option>
      <option value="State1">State 1</option>
      <option value="State2">State 2</option>
    </select>
    <input type="text" placeholder="Landmark (Optional)">
    <input type="text" placeholder="Alternate Phone (Optional)">
  </div>

  <div class="address-type">
    <label><input type="radio" name="addressType"> Home</label>
    <label><input type="radio" name="addressType"> Work</label>
  </div>

  <div class="btn-row">
    <button class="btn-save">SAVE</button>
    <button class="btn-cancel">CANCEL</button>
  </div>
</div>

</body>
</html>